# HTML: Quais ferramentas estão sendo usados?
Usei H1,H3, para colocar textos/subtextos, sobre oq vou relatar em meu site.
Coloquei links para adicionar mais informações imagens,notícias, mais clareza no meu site.
Dentro da minha Div coloquei <a> e coloquei meus index dentro, conectando todos em um só lugar repetindo em todas as páginas.
Coloquei Br para pular linha e organizar a página e coloquei também (p) para adicionar parágrafos para colocar mais informações organizadamente.
E outra coisa que coloquei dentro da Div foi <ul> e <li> para fazer uma lista não ordernada no meu site.

# CSS: Quais propriedades estão sendo usadas?
Tenho o (.Navbar) na div para dar cor no texto dentro dela, e o (.Navbar a) com a mesma função só que em um lugar diferente.
#título adicionei cor nele através do color.
header que serve para organizar e fazer cabeçalho, já o header h1 para arrumar expecíficamente o H1.
criei um formulario e a lista para organizar, tamanho, espaço, cor, entre outras coisas.

 margin: 0;   (a margem do elememento) 
    padding: 0;   (é o preenchimentp )
    box-sizing: border-box;   (tamanho da borda)

     overflow: hidden;   (oculta qualquer conteúdo que ultrapasse os limites de um elemento)
    background:  #f0e8c2 ; (a cor do corpo do site)
    color: white; (a cor da letra)

display: flex; ( deixa os elementos dispostos e alinhados de forma flexível e organizada)
    flex: 1; (flexibilidade a organização )
    gap: 16px; (o espaçamento entre os itens )
    width: 50%; (muda o tamanho do elemento)
    justify-content: space-between; (alinha itens em um contêiner flexível, distribuindo o espaço igualmente entre eles.)
    margin-bottom: 8px; (a margem da tag button)
    align-items: center; (alinhar algo no centro)


# JS: Há algum recurso Javascript 

const: (cria uma variável constante)
function: (Chamar o construtor diretamente pode criar funções dinamicamente.)
let: (Declara uma variável local no escopo do bloco atual, opcionalmente iniciando-a com um valor.)
if: (A condicional if é uma estrutura condicional que executa a afirmação, dentro do bloco, se determinada condição for verdadeira.) 
else: (Se for falsa, executa as afirmações dentro de else.)
